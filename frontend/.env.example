# Copiar a .env.production.local (o setear como variable de entorno en el
# host de hosting) antes de correr `npm run build`. CRA incrusta este valor
# en el bundle en tiempo de build — no es configurable en runtime después.
REACT_APP_API_URL=https://tu-backend.tudominio.com/api
